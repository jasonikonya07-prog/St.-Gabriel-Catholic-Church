{
  "name": "st-gabriel-catholic-church-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm --prefix backend ci && npm --prefix frontend ci && npm --prefix frontend run build"
  },
  "dependencies": {
    "serverless-http": "^3.2.0"
  }
}
